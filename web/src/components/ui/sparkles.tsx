// Sparkles 组件因 @tsparticles 版本兼容问题未启用；保留空模块以避免类型检查报错。
// 如需启用粒子背景，请对齐 @tsparticles/react 与 @tsparticles/slim 的版本后再恢复实现。
// 导出占位组件以满足 cover.tsx 的引用（cover 当前未被使用）。
export function SparklesCore(_props: Record<string, unknown>) {
  return null;
}
