# 项目级 npm 配置（勿删本文件）
#
# 用户级 npm 配置把 proxy / https-proxy 指向了 127.0.0.1:7890（一个已停用的本地代理），
# 会让 npm install 卡死。这里用**空值**覆盖它 —— 实测 `npm config get proxy` 在 web/ 下
# 返回空（覆盖生效）。所以下面两行是**功能性**的，不是冗余：删掉会把
# "安装卡在已停代理"的老问题带回来。
#
# 代价：npm 每次会打印
#   npm warn invalid config proxy="" ... Must be full url with "http://"
# 这是**预期且无害**的噪声（npm 视空值为非法配置，但仍按空值生效）。
# CI 环境没有用户级代理，同样只是多打几行警告。
proxy=
https-proxy=
registry=https://registry.npmjs.org/
