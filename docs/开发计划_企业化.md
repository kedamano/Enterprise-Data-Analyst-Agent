# 企业级数据分析师 Agent · 企业化开发计划（SDD × TDD · 逐日推进）

> 版本 v1 · 2026-09-09 · 单人全职口径（团队可等比压缩天数）
> 目标：把当前「工程护栏齐全但能力偏模板」的 Agent，推进到「分析师愿意天天用」的企业级水平。
> 依据：`docs/审计报告.md`（现状）、`docs/对标企业级Gap.md`（工程缺口）、`docs/分析师视角缺陷.md`（待建）、DeepAnalyze 借鉴（自由写码）。

---

## 0. 总纲

### 0.1 愿景与范围
- **愿景**：分析师用自然语言完成**可信、可溯源、可复用**的数据分析；Agent 能**自由写 SQL/Python 执行**，但**每个数字都能点回底层查询**。
- **本期范围（8 周单人）**：Phase A（能力主线）+ Phase B（信任/工程主线）。
- **非目标（明确不做，避免范围蔓延）**：自训模型、多 Agent 平台、GraphRAG、指标语义层重系统（标为 C 里程碑）、BI/调度平台。

### 0.2 铁律（每条对应真实踩过的坑）
1. **规格先行**：任何改动先写 `docs/specs/<N>-<name>.md` 的契约段（输入/输出/失败/边界/权限/可观测），再写代码。
2. **先红后绿**：先写会失败的测试（含负路径），Mock 过 → 再谈真模型。
3. **防假绿**：conftest 强制 `MOCK_LLM=false + LLM_NO_FALLBACK=true`；真实模型套件单独存在；任何静默降级判失败。
4. **数字可溯源**：新增的每个"结论级数字"必须可追到 SQL/step_id（Epic E1）。
5. **改动不破坏基线**：每次收工跑一次回归集 + `python -m app.eval.runner --mode mock`，记入 `docs/progress/DailyLog.md`。
6. **真实模型待验清单**：凡依赖真模型质量的项（写码正确率、统计判断），无 key 前标 `[待真实验证]`，不得声称已达标。

### 0.3 每日循环（Ritual，每天照做）
```
① 读卡：取 docs/开发计划 当日任务卡 + docs/progress/DailyLog 昨日断点
② SDD：更新/新增 docs/specs/<当日>.md（契约：输入/输出/失败/边界/权限/可观测）→ 必要时补 schema/prompt
③ TDD 红：写 tests/<对应>.py 失败用例（正路径+负路径），跑红（不绿不算写完）
④ 实现 → 绿（Mock 全过）
⑤ 回归门禁：pytest 关键套件 + eval mock；真模型项记 [待真实验证]
⑥ 收工：更新 DailyLog（新增用例数/通过数/回退/下一日待办）
```

---

## 1. 路线图（Phase 门禁 + 逐日卡）

| 阶段 | 主题 | 天数 | 门禁 |
|---|---|---|---|
| P0 | 基线对齐 + 当日工具链 | D1–D2 | 回归基线可复现、DailyLog 模板就绪 |
| A-1 | **E1 数字溯源**（先做，信任前提） | D3–D7 | 报告 claim 带 sql_id；/debug 点开→SQL→结果 |
| A-2 | **E2 自由写码**（SQL/Python 由模型产出） | D8–D13 | 沙箱执行自定义分析端到端 demo；负路径全过 |
| A-3 | **E3 中间结果迭代**（改口径不重跑） | D14–D17 | 多轮"基于上一结果下钻/改期"不重跑全链 |
| Gate-1 | 复盘 + 演示脚本 | D18 | 3 分钟演示：溯源→写码→迭代 |
| B-1 | **E4 质量/口径 + 数据脱敏开关** | D19–D23 | 质量基元；行列脱敏默认开 |
| B-2 | **E5 统计严谨 + 导出** | D24–D26 | 显著性/样本声明；导出 SQL/CSV |
| B-3 | **E6 真实评测 + 成本轻路径** | D27–D30 | eval real 基线；小迭代直连轻路径 |
| B-4 | E7 多源 + 部署硬化（镜像瘦身/鉴权说明） | D31–D34 | 双数据源；prod 镜像 <2GB |
| Gate-2 | 收口 + 待验清单 | D35–D36 | 文档/测试/指标归档 |

> 若遇真实模型 key 到位：把 B-3 提前并行；E2/E5 的 `[待真实验证]` 项在该日转真实跑。

---

## 2. 各 Epic 规格要点（SDD 侧）与 TDD 侧交付

### E1 数字溯源（D3–D7）
- **SDD 契约**：`AnalysisResult.findings[].evidence[]` 增加 `sql_id/sql_text_hash`；`ToolResult` 已带 `step_id`；报告渲染时 claim ↔ 查询一一映射；新增 `/api/v1/chat/analyze/trace/{session}`（或复用 `/debug/traces`）返回"claim → 所用查询 → 行样本"。
- **TDD**：
  - `test_evidence_carries_sql_id`：每个含数值的 finding 的 evidence 能链到一条 sql_query step。
  - `test_report_claims_resolve_to_query`：报告里每个数字都能解析出底层查询（mock 确定性）。
  - 负路径：finding 无数值则不要求溯源；无证据断言 fail（复用 eval `must_not`/规则）。
- **关联**：`state.py`/`analyst` prompt、`report_tool`、`nodes._to` step 事件。

### E2 自由写码（D8–D13）—— 分析师痛点 ② + Gap P0-1
- **SDD**：Analyst 阶段允许模型产出自定义分析步骤，两种形态：`sql_text`（SQL lint + 只读守卫，复用 `sql_tool` 白名单）或 `python_code`（AST allowlist + 沙箱，复用 `python_tool`）；新增"人审可选"`confirm` 字段；产物与溯源自动挂 step_id。
- **TDD**：
  - 自由 SQL：合法 join/聚合执行并返回（mock 断言非空、行数受限）；**注入/DML 拒绝**（复用负路径）。
  - 自由 Python：生成分析脚本沙箱执行、产物回传；危险模块/逃逸拒绝。
  - 失败回注：执行失败 → 结构化错误 → 下一轮自纠错（已有 REPLAN 反馈通道）。
- **真模型质量**：`[待真实验证]`（key 到位后跑 golden 新增"自定义分析"类用例）。

### E3 中间结果迭代（D14–D17）—— 分析师痛点 ①
- **SDD**：会话内存引入"最近数据集表"（`session_last_dataset`：schema+rows+来源 sql_id），多轮时若新 query 是"对上一结果操作"（下钻/改期/换粒度），跳过前几步直接作用于该表；提供显式"重跑全链"开关。
- **TDD**：`test_iteration_skips_replan_for_prior_dataset`（同 session 第二次只触发目标阶段）；`test_force_full_rerun`；口径切换仍正确（mock）。

### E4 质量/口径 + 脱敏（D19–D23）
- **SDD**：`dataset_profile` 输出加 主键唯一性/join 放大/粒度/日期连续性 检测；输出脱敏分级配置（默认 mask 敏感列，不进 LLM 上下文/仅取样本）；Reflection 增加"口径可比性"检查项。
- **TDD**：profile 新指标正确性；脱敏后 prompt 不含原始行/列值；口径漂移样例被 Reflection 拒。

### E5 统计严谨 + 导出（D24–D26）
- **SDD**：Python 模板允许使用 scipy；报告要求显著性/样本/季节声明（prompt 规则 + schema 字段 `stats_notes`）；`/export`（或 UI 按钮）导出 SQL/CSV。
- **TDD**：显著性代码路径 mock 断言有输出；无样本量结论被拒/标注。

### E6 真实评测 + 成本轻路径（D27–D30）
- **SDD**：eval 增加"自由写码/溯源/迭代" golden 子集；真实模式跑通；新增**轻路径**：可直接由 SQL 回答的小问不走全链（跳 Reflect/Reporter 简版）。
- **TDD**：eval mock 全绿 + `--mode real` 冒烟；轻路径命中判定单测。

### E7 多源 + 部署硬化（D31–D34）
- **SDD**：多 `data_db_url` 命名源；镜像瘦身 `Dockerfile.prod`（目标 <2GB）；安全说明（鉴权/HTTPS）。
- **TDD**：多源参数化用例；`docker build` + 容器冒烟 CI 化。

---

## 3. 全局质量门禁（每次收工必跑）
```bash
python -m pytest tests/ -q --ignore=tests/test_agent_real.py   # 离线全量
python -m app.eval.runner --mode mock                          # 质量/成本基线
# 关键 Epic 完成后另跑对应 *_live / docker_live；有 key 时：
python -m pytest tests/test_agent_real.py -v
python -m app.eval.runner --mode real
```
**DoD（Epic 级）**：规格文档 ✅ → 红绿测试 ✅（正+负）→ 回归与 eval 无回退 ✅ → DailyLog 记录 ✅ → 真模型项已标 `[待真实验证]` 或已验 ✅。

## 4. 基线指标（每周对比）
| 指标 | 记录位置 |
|---|---|
| 回归用例数 / 通过率 | DailyLog |
| eval mock: FINISH/pass/tool_success/avg LLM calls | `app.eval` 输出 |
| 溯源覆盖率（数值 claim→sql_id 比例） | E1 后起统计 |
| 自定义分析步骤成功率（mock/real） | E2 后起统计 |
| 真实成本 USD | eval real（配单价后） |

## 5. 风险与对策
- 真实 key 未到位 → E2/E5 质量不可验：**先建 mock 契约与待验清单**，到位即转真实（对策：把 B-3 前置）。
- 自由写码打开攻击面 → 只读/白名单/AST/沙箱全保留；写码仅在"明确允许的分析步骤"内，默认关闭 flag。
- 迭代语义误判（模型把新问题当旧结果操作）→ 只对显式指代词（"基于上一结果/改为/只看"）触发，否则全链。

## 6. 附件约定
- 规格：`docs/specs/E1..E7/`（每 Epic 一份 md，含契约表）
- 测试：沿用 `tests/`，命名 `test_<epic/unit>.py`
- 进度：`docs/progress/DailyLog.md`（每天新增一节：日期/任务/红绿/回归/待验/明日）
- 本期每天 ① 读当日卡 → ②③④⑤⑥ 循环；建议每天收尾留 15 分钟更新 DailyLog 与任务状态。

---

## 7. 逐日排期明细（D1–D36 · 每天一卡）

> 每日循环见 §0.3。列：Day | 阶段 | 当日任务（SDD → TDD → 验收/门禁）。
> ✅=已完成；所有标注 `[待真实验证]` 的项，真实 key 到位后转真实跑并更新待验清单。

| Day | 阶段 | 当日任务与验收 |
|---|---|---|
| **D1** ✅ | P0 | 离线回归基线 169 passed；eval mock 基线（FINISH/pass/工具/LLM/成本）；**发现并修 Redis 故障使 short_term 崩溃**（读路径兜底+负路径测试）；DailyLog 启用 |
| D2 | P0 | 工具链巡检：跑一遍「红→绿」最小样例校准每日循环；E1 规格评审定稿（`docs/specs/E1/01`）；补一条已知待验项清单 |
| D3 | A-1 E1 | **SDD**：state/schema 加 `Evidence.sql_id/sql_text_hash/row_sample`，analyst.md 加溯源约束；**TDD 红**：数值 finding 无 sql_id 判 FAIL、有 sql_id 指向真实 sql step |
| D4 | A-1 E1 | **实现**：analyst 后处理自动给 evidence 补 sql_id（指向最近 sql step）；**绿**；跑记忆/编排回归 |
| D5 | A-1 E1 | reporter/report_tool 渲染 `[src:step_id]`；新增 `GET /api/v1/chat/analyze/trace/{session}`（claim→query→样本）；SSE FINISH 附 trace 摘要；红绿 |
| D6 | A-1 E1 | eval 增加「溯源」断言维度（must_trace / 禁止编数）；溯源覆盖率统计进基线表；回归全绿 |
| D7 | A-1 E1 | **E1 门禁**：示例报告每条数值可点开 SQL；无证据 fail 用例过；DailyLog 归档 |
| D8 | A-2 E2 | **SDD E2 定稿**：Analyst 可产自定义 `sql_text`/`python_code`（守卫全保留）+ 可选 `confirm`；**TDD 红**：自由 join SQL 可执行、注入/DML 拒绝 |
| D9 | A-2 E2 | **实现** executor 支持 `sql_text` 分支（lint→只读→执行→回注 step）；负路径（写库/单语句违例）绿；回归 |
| D10 | A-2 E2 | **实现** `python_code` 自由生成分支（模型出码→AST 守卫→沙箱→产物）；危险模块/逃逸负路径绿 |
| D11 | A-2 E2 | 失败自纠错闭环：自由码执行失败→结构化错误→下一轮重写；mock 测试三连败后成功 |
| D12 | A-2 E2 | UI/API 展示自定义分析产物+溯源；eval 增「自定义分析」mock golden；负路径全量回归 |
| D13 | A-2 E2 | **E2 门禁** + 列出 `[待真实验证]`（自由码质量依赖真实模型）；DailyLog |
| D14 | A-3 E3 | **SDD E3 定稿**：会话「最近数据集表」+ 指代词触发；state 增 last_dataset；**TDD 红**：同 session 指代上一结果时不重跑全链 |
| D15 | A-3 E3 | **实现**：下钻/改期/换粒度命中 last_dataset → 只执行目标阶段；force_full_rerun 开关；红绿 |
| D16 | A-3 E3 | 口径切换正确性负路径（未指代时仍全链）；回归 + eval 回归 |
| D17 | A-3 E3 | **E3 门禁** + 3 分钟演示脚本草稿（溯源→写码→迭代主线）；DailyLog |
| D18 | Gate-1 | 演示 dry-run：脚本化跑通三条主线；产出 `docs/progress/demo-script.md`；记录阻塞项 |
| D19 | B-1 E4 | **SDD E4 定稿**：profile 增主键唯一/join 放大/粒度/日期连续性；脱敏开关（默认开）；reflection 口径可比检查；**TDD 红**（主键唯一判定） |
| D20 | B-1 E4 | 实现 profile 新质量基元；多表 join 放大样例红绿；回归 |
| D21 | B-1 E4 | 脱敏：敏感列默认脱敏、样本不入 LLM 上下文；负路径（断言脱敏后无原始值）绿 |
| D22 | B-1 E4 | reflection 增「口径可比/同环比基线」检查项；eval 增口径 golden；回归 |
| D23 | B-1 E4 | **E4 门禁**：profile 质量报告 + 脱敏生效断言；DailyLog |
| D24 | B-2 E5 | **SDD E5 定稿**：python 模板可用 scipy；报告要求 stats_notes（显著性/样本/季节）；**TDD 红**（显著性代码路径输出） |
| D25 | B-2 E5 | 实现 stats 说明字段 + 导出端点 `GET /export?session=`（SQL/CSV/报告）；UI 按钮；红绿 |
| D26 | B-2 E5 | **E5 门禁**：无样本量结论被拒/标注用例过；轻路径(§E6) 设计备忘；回归 |
| D27 | B-3 E6 | eval golden 扩展（自定义/溯源/迭代子集）mock 基线；全绿记录 |
| D28 | B-3 E6 | **轻路径**：可直连 SQL 回答的小问跳过 Reflect/Reporter 重链；命中判定单测 |
| D29 | B-3 E6 | 真实 key（若到位）：`eval --mode real` 冒烟 + 待验项转真实；否则 mock 基线 + 文档 |
| D30 | B-3 E6 | **E6 门禁** + 成本单价配置说明；DailyLog |
| D31 | B-4 E7 | **SDD E7 定稿**：多数据源命名；`Dockerfile.prod` 瘦身清单（目标 <2GB）；**TDD 红**（多源参数化） |
| D32 | B-4 E7 | 多源支持实现与用例；回归 |
| D33 | B-4 E7 | Dockerfile.prod 构建 + 容器冒烟 + CI 脚本（build→health→analyze）；记录镜像体积 |
| D34 | B-4 E7 | 安全说明（鉴权/HTTPS/多租户）收口；`docs/部署上线.md` 更新 |
| D35 | Gate-2 | 全量回归 + eval（real 或待验）；溯源覆盖率等指标归档 `docs/progress/metrics.md` |
| D36 | Gate-2 | 收口：README/面试稿刷新；待验清单清点；计划 v2（含 C 里程碑：语义层/血缘/RBAC/DLP 排期） |

### 里程碑
- **M1（D18）**：分析师主线的可演示 MVP（溯源+写码+迭代）。
- **M2（D30）**：信任主线（质量/脱敏/统计/真实评测）基线建立。
- **M3（D36）**：多源+部署硬化 + 全部门禁归档；形成「企业级 v2」起点。
- **真 key 前置**：E2(D13)/E5(D26)/E6(D29) 的 `[待真实验证]` 统一在 key 到位后于对应日转真实。

---

## 8. 计划 v2（2026-09-11 · Gate-2 前置收口）

### 8.1 实际完成情况（以仓库实况为准）

| 阶段 | 计划日 | 状态 | 交付 |
|---|---|---|---|
| P0 基线 + 工具链 | D1–D2 | ✅ | 回归基线、DailyLog 循环、Redis 故障修复 |
| A-1 E1 数字溯源 | D3–D7 | ✅ | `sql_id/sql_text_hash/row_sample`、`/trace/{session}`、覆盖率 1.0 |
| A-2 E2 自由写码 | D8–D13 | ✅ | 自由 SQL/Python、AST+沙箱、失败自纠错、（+ROUTE 输出路由） |
| A-3 E3 中间结果迭代 | D14–D17 | ✅ | `last_dataset`、指代识别、目标阶段、force_full_rerun、守卫 |
| Gate-1 演示 | D18 | ✅ | `docs/progress/demo-script.md` + 402 阻塞定位 |
| B-1 E4 质量/口径/脱敏 | D19–D23 | ✅ | **01 质量基元 / 02 脱敏 / 03 口径可比 / 04 门禁 / 05 方法论** |
| B-2 E5 统计 + 导出 | D24–D26 | ✅ | `stats_notes`、对抗性门禁、**scipy**、`GET /export`（zip/sql/report/csv）+ UI 按钮 |
| B-3 E6 真实评测 + 轻路径 | D27–D30 | ⚠️ 部分 | 15 条 golden（7 条 `requires_real` 门控）+ 结构化断言 + 轻路径；**real 基线待 key** |
| B-4 E7 多源 + 部署硬化 | D31–D34 | ✅ | 命名数据源、`Dockerfile.prod`、冒烟脚本、安全说明（**镜像体积未实测**） |
| Gate-2 收口 | D35–D36 | ✅ | `metrics.md` 归档、README/部署文档刷新 |

### 8.2 计划外新增（源于 `docs/测试用例.md` 300 条基准反查）

| Epic | 内容 | 规格 |
|---|---|---|
| CLARIFY | 澄清回路（反问而非报错终止；前后端闭环 + 续跑不死循环） | `CLARIFY/01` |
| SEMANTIC | 业务语义层（维表枚举/键推断 → 注入 context/planner/analyst） | `SEMANTIC/01` |
| DEGRADE | 降级可见化（`degraded`/`llm_fallbacks`/`/health/llm`）+ 中间件快速失败 | `DEGRADE/01-02` |

### 8.3 未完成 / 受阻（如实记录）

1. **真实模型验证**：7 条 `requires_real` golden、写码质量、语义/统计判断 → `pending-real.md`（卡余额 402，非工程问题）。
2. **生产镜像体积**：本机三个 Docker 镜像源全不可用（`daocloud` EOF / `nju` 403 / `azk8s` 不可达），
   `<2GB` 目标与容器内冒烟**未实测**。
3. **口径语义判读**：`unit_mismatch`（万元/亿元混用）与 `filter_mismatch`（含/不含退款）只有枚举值，未实现确定性判定。

### 8.4 C 里程碑（下一期候选，按价值排序）

1. **指标语义层（重版）**：指标血缘、口径注册表、同环比基线自动判定 —— D47 落地 `lineage.py`（`metric→口径→sql_id→SQL→表/列`，解析产物标 `parsed_from_sql` 不冒充元数据），D48 落地 `caliber_registry.py`（CRUD + JSON 文件后端）+ `infer_baseline_period`（环比/同比确定性基线推断）+ 两个披露级 issue（`baseline_mismatch`/`caliber_deviation`）。**残留**：口径语义判读的 LLM 维度、真实基线上 `sql_id` 多为 null。
2. **真实评测闭环**：`eval --mode real` 基线 + 成本单价 + golden 扩充（含"自定义分析"类）。
3. **多源进阶**：跨源联邦查询（下推/物化中间表）—— 现明确不做。
4. **DLP / 更细粒度脱敏**：D49 已落地——按角色/字段级策略（`dlp_policy`，权限高于策略）、HMAC 可验证水印、导出审批流（脱敏版安全默认 / 原始版走 HITL）。**残留**：水印在真实接收方校验链路未验；列名模式需按客户数据字典定制。
5. **UI 能力对齐**：增量徽标、质量横幅、澄清卡片已做；待补指标卡与导出预览。

### 8.5 铁律执行情况
六条铁律全部执行；本轮**新增一条实践教训**（记入 DailyLog）：
**含引号/转义的文本一律用 Edit 工具改，不要走 heredoc 脚本**——本轮因此类转义问题返工三次。

---

## 9. 逐日修复排期（D38 起 · 2026-09-13 排定）

> **排期依据**：`docs/对标企业级Gap.md`（回写后仍成立的 P0/P1 + 13 条残项）、
> 本文件 §8.3/§8.4、`docs/审计报告.md`（Round 1–9 已收口，无遗留）。
> **D1–D37 已完成**（D37 见 DailyLog：首个真实基线 + 两个假绿 + 文档回写）。
>
> **每日循环不变**（§0.3）：① 读当日卡 → ② **SDD 定契约** → ③ **TDD 先红** → ④ 实现转绿
> → ⑤ 跑全量回归 + eval 基线 → ⑥ 追加 DailyLog 一节。
> **DoD**：规格 ✅ / 红绿用例（正+负）✅ / 无回退 ✅ / DailyLog ✅ / 真模型项标 `[待真实验证]` ✅。

### 9.1 排期总表

| Day | 阶段 | 当日任务 | 验收（可判定） |
|---|---|---|---|
| **D38** ✅ | C-1 评测可信 | ~~真实基线复跑 + 溯源覆盖率"无从度量"~~ **已于 2026-09-13 完成** | 见 DailyLog D38：根因是 **analyst 输出"能过校验但不可用"被静默当成功** + 执行器未补 schema 发现。修后 **工具成功率 0.36→1.0**、findings 全 0→**非 0**、断言 **0.429→0.143**（去掉两类假绿）。**溯源仍 0/0**，但原因换成"发现停在元讨论"（诚实读数）→ 由 D39 固化 |
| **D39** ✅ | C-1 | ~~badcase 回流~~ **已于 2026-09-13 完成** | `app/eval/badcase.py`（落盘/去重/复跑/草稿）+ CLI + runner `--badcases`；并**从真实 badcase 固化出 `min_numeric_claims`**（补 E1 在零 claim 时恒真的洞）。见 `E6/01` §6 |
| **D40** ✅ | C-1 · **插入** | ~~LLM 调用挂住检测~~ **已于 2026-09-14 完成**：根因是 **httpx 的 read timeout 会被数据块重置**——上游周期性吐 keep-alive 时永不触发（有"心跳"的挂起）。修法：与传输无关的**墙钟**（daemon 线程 + join）+ 不可重试 + 留痕；配置 `LLM_HARD_DEADLINE_S`（须 > `LLM_TIMEOUT_S`） | `tests/test_llm_hang_guard.py` **8**（用永不 set 的 Event **确定性**复现，不靠 sleep 竞态）；回归 904 passed；mock 基线不变。**诚实边界**：僵尸线程仍会累积（无法强杀），属"致命伤降级为慢性病" |
| **D41** ✅ | C-1 | ~~幻觉率监控 + 离线-在线一致性~~ **已于 2026-09-14 完成**：先查出**"已溯源"有两个定义**（离线 `trace_counts` 按"能 resolve 到真实 SQL step"，在线 `trace_manifest` 按"sql_id 非空且有 SQL"），**无任何测试比对** → 引入**唯一口径** `sources.trace_coverage()`，两边都走它 | `tests/test_hallucination_rate.py` **12**（含正/负样例各 1 条 + "同 state 离线值 == 在线值"）；指标进 eval 报告 + `/metrics`（counter+gauge）+ 告警规则；**零 claim → None 不是 0**（不自欺）。回归 916；mock 基线 溯源 13/13 / 幻觉率 0.0 |
| **D42** ✅ | C-2 上下文 | ~~prompt 预算强制~~ **已于 2026-09-14 完成**：先核实 `fit_to_budget` **只在测试里被调用过**（app/ 零命中）——"实现了但从未接线"；`build_user_message` 一直 `json.dumps` 无上限。新增 `prompts/budget.py`：**序列化前**按优先级压缩（行数据减半 → 丢最旧工具结果 → 只留 context/plan），说明写进 prompt 的 `<context_budget>` 块 | `tests/test_prompt_budget.py` **8**（含"压缩后 JSON 围栏仍合法"与"用户问题永不截断"）；**测试当场抓到 `_shrink_rows` 的 `while` 死循环**（长度不变却报有进展）。回归 924；mock 基线不变（16000 不动正常链路） |
| **D43** ✅ | C-2 | ~~单会话 token 熔断~~ **已于 2026-09-14 完成**：熔断点选 `router._call_model`（全部 LLM 调用含 judge 的**唯一咽喉**）；语义是"**拒绝下一次**"而非事后报警（检查在调用前、且在 tenacity 重试**内部**，否则重试可绕过）；不可重试 + 留痕 | `tests/test_session_token_budget.py` **12**（含并发 1600 次不丢、端到端"3 次后熔断且不再发请求"）。**测试抓到真缺陷**：记账曾写在 `if usage is not None` 里 → 不传 usage 的路径（judge）烧了 token 却不计数，熔断永不触发。**边界如实写明**：按 run 而非跨轮会话（透传 session_id 要动全部调用点，本日不做）。回归 936 |
| **D44** ✅ | C-2 | ~~多租户隔离测试矩阵 + 日志采样/SLI-SLO~~ **已于 2026-09-14 完成**：① 租户矩阵 4 面 × 2 腿（跨租户不可见 + **同租户可见**，后者防「功能全坏也算隔离」的假绿）+ 空租户=全局的兼容性；② 日志采样（**只采正常、失败永不采样**；0/负数退回不采样而非全丢）；③ 6 条 SLI/SLO + 三条口径纪律；**顺带补 `llm_fallbacks_total`**（降级率此前算不出来） | `test_tenant_matrix.py` **9** + `test_log_sampling.py` **10**；回归 955；mock 基线不变。**待补如实记录**：采样后的日志未接入聚合（可观测性分辨率下降） |
| **D45** ✅ | C-3 合规 | ~~两步授权 HITL~~ **已于 2026-09-14 完成**：策略引擎 `security/hitl.py`（**默认拒绝 + 显式白名单** `_SAFE`；**fail-closed**；凭证不可推导 + 常量时间比较）+ **一次性放行**（确认只对"这一次"生效，取走即失效）+ `POST /analyze/confirm` + 导出网关（428 下发凭证） | `tests/test_hitl.py` **19**（策略/流转/审计/放行 + 端到端 API 三例）；回归 974；mock 基线不变（**默认关 → 零影响**）。**实现中两个真问题**：① 确认后不给放行 → 调用方被反复拦住；② 放行按动作铺键 → 测试互相污染（改用单一字典键）。**写测试时自己的两条用例互相矛盾**，逼出"默认拒绝 + 显式放行"的设计 |
| **D46** ✅ | C-3 | ~~审计落库（SQLite/PG）+ SQL 查询接口~~ **已于 2026-09-14 完成**：四条流（tool/auth/masking/hitl）统一经 `security/audit_store.py`，后端三选一（**jsonl 默认=内容一字不变** / sqlite / postgres）；**不做双写**（避免"以哪份为准"）；`import_jsonl()` 用**内容指纹保证幂等**；`GET /debug/audit` | `tests/test_audit_store.py` **14**（含**源码级**钉住四条流都经统一收口——防"切了后端只有部分生效"的老毛病）；`path` 覆盖参数保住既有 monkeypatch。回归 988；mock 基线不变 |
| **D47** ✅ | C-4 语义 | ~~指标血缘~~ **已于 2026-09-14 完成**：补齐 E1 断掉的那一截——`metric → 口径 → sql_id → SQL → 表/列`。`lineage.py` 纯函数（表名**复用 gate 的解析**，同口径）；表/列标 `confidence="parsed_from_sql"`（解析产物不冒充元数据）；无 `sql_id`/悬空 → `traced=False` + 原因，**绝不借来源** | `tests/test_metric_lineage.py` **13**（列解析四坑：`sum` 是函数非列 / `f.revenue` 归一 / `AS x` 是别名 / WHERE 字面量先抹）。`GET /analyze/lineage/{sid}`。回归 **1001**（首次过千）；mock 基线不变。**实测提醒**：真实基线上 `sql_id` 多为 `null` → 大部分指标会 `unresolved`，这是**如实反映**而非缺陷 |
| **D48** ✅ | C-4 | ~~口径注册表 + 同环比基线自动判定~~ **已于 2026-09-14 完成**：`caliber_registry.py`（`CaliberSpec` + JSON 文件 CRUD + 原子写 + 主键幂等）+ `infer_baseline_period`（环比→上一完整自然月/等宽回平移；同比→去年同月/去年同日期；2/29→2/28；解析不出→None）+ 两个**披露级** issue（`baseline_mismatch`：声明了环比/同比但基线缺/错；`caliber_deviation`：报告口径与登记口径单位/限定词极性冲突）。注册表读故障绝不打断 `caliber_check`。`GET/POST/DELETE /analyze/caliber` CRUD 端点 | `test_caliber_registry.py` **10**（CRUD 7 + 故障不打断 1 + API 2）+ `test_caliber_baseline.py` **17**（基线推断 8 + baseline_mismatch 联动 4 + caliber_deviation 联动 4 + 畸形不抛 1）；回归见 DailyLog D48；eval mock 基线不变（FINISH 1.0 / 溯源 1.0 / 幻觉率 0.0） |
| **D49** ✅ | C-4 | ~~DLP 细粒度脱敏~~ **已于 2026-09-14 完成**：`security/dlp.py`（**角色×字段**级别解析——`Principal.denied_columns` 权威 > 角色 `deny_columns` > `column_levels` **最长 key 优先** > 角色 `default_level` > 回退全局 `mask_level`；解析异常不 fail-open）+ `security/watermark.py`（**HMAC-SHA256** 可验证水印，签不出=验不出）+ 导出集成（`masked=1` 脱敏版是**安全默认**无需审批；`masked=0` 原始版走 D45 HITL 审批；水印=新文件+响应头，**不动既有产物字节**） | `tests/test_dlp.py` **19**（策略 8 + CSV 2 + 水印 5 + 导出集成 4）；回归见 DailyLog D49；eval mock 基线不变（FINISH 1.0 / 溯源 1.0 / 幻觉率 0.0）；**默认零影响**（`DLP_POLICY` 空 + 无 secret → 导出行为与 D48 一字不变，既有 56 个导出/HITL/鉴权/脱敏用例全绿） |
| **D50** | C-5 生态 | **MCP 接入层**：把只读工具按 MCP 协议暴露（可发现/可授权/可观测） | MCP server 可被客户端列出工具；权限沿用 AUTH/01 |
| **D51** | C-5 产品 | **报告图内嵌 + UI 指标卡 / 导出预览** | 报告含图片引用且前端渲染；`npm run build` 通过 + 源码级契约 |

### 9.2 外部阻塞（**不排日，条件就绪即插队**）

| 项 | 卡在哪 | 就绪后可做 |
|---|---|---|
| **真实业务库验证**（Gap P0-1） | 缺真实库/真数据（用户提供） | 接一次真库跑真实业务问句；复核 `bench_scale.py --backend postgres/mysql` |
| **镜像 <2GB 实测 + 镜像仓库**（Gap P0-3） | 本机镜像源全不可达（daemon 正常） | `docker build -f Dockerfile.prod` + `scripts/smoke_container.sh`；CI `image` job 已备 `<2GB` 硬断言 |
| **长任务异步化**（Gap P1-5） | 价值取决于是否真上生产 | 异步任务 + 回调，替代长 SSE |

### 9.3 排期原则

1. **先修"测量本身"再修"被测对象"**：C-1 排在 C-2/C-3 前面——真实基线若有假绿，
   后面所有质量指标都建在沙子上（D37 刚踩过：工具成功率 1.0 是假的）。
2. **每天一张卡，宁可小**：一天做完 SDD+TDD+绿+回归+D工志是既有节奏；
   卡片过大就拆（如 D41/D42 本是同一议题，拆开是因为各自都要红绿+回归）。
3. **阻塞项不占日**：需要外部条件的不预先排日，避免每天"卡在一张做不了的卡上"。
4. **每完成一天，回写 `对标企业级Gap.md` 对应行的 `状态` 列**——防止它再次变成过时清单。
